# KGBA Priority Zero — P0.2 Spin Stop

Repo Forge overlay. Import at the **repository root** after loading the existing `KGBArcade/main` repository.

Adds the canonical Spin Stop v1.0.0 release at:

- `/games/spin-stop/`
- `/games/spin-stop/play/`

Also updates `games.json`.

After import, apply the small root-homepage edits in `docs/release-patches/P0_2_SPIN_STOP_INDEX_PATCH.md`, review the resulting tree/diff, and commit.

Suggested commit message:

`P0.2: release Spin Stop v1.0.0`
